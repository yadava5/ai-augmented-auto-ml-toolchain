/**
 * Subtle horizontal rule between sidebar subtab groups (e.g. data vs context files, plans vs chats).
 */
export function SidebarSubtabSectionDivider() {
  return <div className="my-1 mx-3 border-t border-border/50" />;
}
