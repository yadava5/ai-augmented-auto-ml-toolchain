# Suite Manifests

Tracked benchmark suite manifests live here.

Suites may reference datasets that are still `pending`, but scored execution must
refuse to run those entries until the referenced dataset manifests are `staged`.

See [`../README.md`](../README.md) for the catalog contract.
