# Public Staged Dataset Bytes

Public benchmark datasets are staged locally under this directory and stay out of git.

Keep only this README committed. The benchmark preparation step will create dataset-specific subdirectories here.
