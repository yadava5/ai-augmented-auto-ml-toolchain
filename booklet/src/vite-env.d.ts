/// <reference types="vite/client" />

// Types for `import.meta.env`, which TeamPage.tsx and ScreenshotPlaceholder.tsx
// read to build public-asset URLs from the configured base. tsconfig.json only
// includes src/, so the reference has to live here rather than beside
// vite.config.ts.
